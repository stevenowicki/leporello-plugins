# [CLAUDE-PLACEHOLDER:title]

## Producer's ask
[CLAUDE-PLACEHOLDER: what the producer came in wanting — verbatim or close]

## Editorial framing
[CLAUDE-PLACEHOLDER: what this map shows and why it matters geographically — what the regions/markers represent, what it's NOT claiming]

## Template + rationale
Map (v1 DLS restyle) — chose because **the story is geographic** and the presenter wants to pan/zoom and telestrate on the map live. The DLS restyle keeps the map as an *ambient* full-bleed layer and lifts the storytelling into a small set of dominant featured labels plus a first-class legend, so the eye finds the point in under 3 seconds. [CLAUDE-PLACEHOLDER: justify — e.g., "alignment is best shown as colored territory the presenter can draw over."]

## Content decisions
[CLAUDE-PLACEHOLDER: which 2–3 featured labels carry the story (the dominant tier) vs. which recessed orientation labels only orient; which regions and why those boundaries; the color encoding (what each color means). Per the DLS + broadcast standards: don't encode meaning in color alone — the legend + presenter's words carry it. No on-frame source strip (attribution is metadata only, direction §5.14).]

## Geometry + basemap decisions
[CLAUDE-PLACEHOLDER: where the GeoJSON came from (described/traced/supplied), simplification level. Which basemap style and why (dark dataviz default). If a MapTiler key is inlined, note that it's origin-restricted.]

## Source decisions
[CLAUDE-PLACEHOLDER: what backs the territorial/positional claims — control of an area, an event location, etc. Boundaries are editorial claims; source them.]

## Iteration log
- v1 ([CLAUDE-PLACEHOLDER:date]): initial publish — [CLAUDE-PLACEHOLDER: one-line summary]
